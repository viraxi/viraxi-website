# Viraxi — Official Website

Minimal, modern landing page built with Next.js, TypeScript and Tailwind CSS.

Recent improvements:
- Added `Inter` font for better typography.
- Improved hero copy and CTAs, plus a concise About and Contact section.
- Added `robots.txt` and `sitemap.xml` for SEO.
- Enhanced metadata (Open Graph and Twitter card) and `metadataBase` configured for `https://viraxi.tech`.

## Local dev

1. Install dependencies:

   npm install

2. Run dev server:

   npm run dev

3. Open http://localhost:3000
